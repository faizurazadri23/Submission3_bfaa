<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
   xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:menuCategory="container">
        <item
            android:id="@+id/favorite_bar"
            android:title="@string/favorit"
            android:iconTint="@color/white"
            android:icon="@drawable/icon_favorit"
            app:showAsAction="always"
            />

        <item
            android:id="@+id/pengaturan_bar"
            android:title="@string/pengaturan"
            android:icon="@drawable/icon_pengaturan"
            app:showAsAction="always"/>

    </group>

</menu>